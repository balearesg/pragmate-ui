import { Code } from 'pragmate-ui/code';
import { Results } from './results';
import { implementation } from './implementation';
import { CheckboxGroupExample } from './checkbox-group-example';

# Checkbox

El componente `Checkbox` permite a los usuarios seleccionar una o varias opciones de una lista mediante la marca de
casillas.

### Import :

<Code>{`import { Checkbox } from 'pragmate-ui/form';`}</Code>

### Component :

<Code>{`<Checkbox onChange={handleChange} checked={selectedValue} name="check" />`}</Code>

### Implementation :

<Code>{implementation}</Code>

Result :

<Results />

## CheckboxGroup

<CheckboxGroupExample />
