import { Code } from 'pragmate-ui/code';
import { implementation } from './implementation';

# Form

El componente `Form` permite a los usuarios poder crear un elemento form como cualquier otro pero con la particularidad
de que en su funcion onSubmit no sea necesario agregar la funcion `Event.currentTargert()`.

### Import :

<Code>{`import { Form } from 'pragmate-ui/form';`}</Code>

### Implementation :

<Code>{implementation}</Code>
